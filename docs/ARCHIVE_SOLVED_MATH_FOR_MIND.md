# Archive-solved math → Zig mind (orientation)

**Source of truth:** `I:\FSOT-Physical-Archive`  
**Pin:** `D1D38A185487B452E470AC68ECE2EB45AEB1CA9CE25FC9BF9564C19633FFBE70`  
**Status (on-disk cert):** GREEN — `VERIFICATION_REPORT.json` · multi-prover · 405-domain science panels  

**Purpose:** What is **already solved mathematically** in the physical archive that the Zig genetic brain must **use**, not re-invent with free FI polish.

---

## 1. What the archive has closed (do not re-solve as free parameters)

| Solved layer | Where | What it gives Zig |
|--------------|--------|-------------------|
| **One scalar law** \(S=K(T_1+T_2+T_3)\) | `ARCHIVE_MATH_AND_VERIFICATION_DEEP_DIG.md` · Lean · vendor pin | Seeds → L1/L2 → fold → measure; **0 free LSQ on law path** |
| **Multi-prover spine** | seven-way bare metal GREEN | Boot scalar triangulated; Lean 0 sorry on claims |
| **405-domain green @ ≤0.5% pooled median** | science panels + API pipeline | Accuracy *machine*: live API → FSOT record → audit margins |
| **64-codon trinary map** | `04_Genetics-Longevity/64_codon_trinary_map.txt` · `codon_manifest.yaml` · SR-ITE driver | PRIMARY/SECONDARY maps; 64 count verified |
| **Biology domain scalar** | `biology_numeric_report.json` | e.g. `biology_domain_scalar_S ≈ 0.4447` under pin |
| **Strict biology empirics** | `biology_strict_empirical.json` | mt operon lengths vs NCBI — tight margins |
| **NeuroLab bio contract** | `neurolab_bio_manifest.yaml` | NEUROSCIENCE→neural fold; pathway max gap **0.15**; thalamic K match |
| **Biological brain derivations** | `FSOT_BIOLOGICAL_BRAIN_MATHEMATICAL_DERIVATIONS.md` | Explicit **expression → \(S_{\mathrm{FSOT}}(D)\) → coupling → pathway gains** |
| **Biophysics public panel** | `biophysics_public_panel_benchmark.json` | Domain panel GREEN under pin |

**Doctrine (usage):** pin → match seeds → **name neuro fold** → bridge observables → **keep domain engine** → measure  
(`FSOT_USAGE_DOCTRINE.md`)

---

## 2. Biological brain math (archive) — the right coupling pattern

From `FSOT_BIOLOGICAL_BRAIN_MATHEMATICAL_DERIVATIONS.md` (not a second theory):

```text
component / region text → DNA proxy / trinary signature (A,G=+1; C,T=−1 spin map)
  → expression domain D_r from BrainSpan-style enrichment
  → S_expr = S_FSOT(D_r)     // same engine, observed fold
  → C_r = S_expr · (0.75 + sample_support + 0.5·Allen_structure_support)
  → G_r, a_r  module gains / amplitudes
  → pairwise control profile g_relay, g_feedforward, … from C_st, A_st, S_st
  → pathway dynamics (thalamic / cortex / memory / cerebellum equations)
  → concordance κ vs priors and observables
```

**Allen in that derivation** is used as **structure/support and concordance**, together with BrainSpan expression — not as “invent free membrane knobs until ISI looks good.”

**Zig analogue (required):**

```text
64-codon map + class ORFs
  → geneExpression (φ, π, e, γ)     // already codon_fixed
  → phenotype knobs                 // genotype_fixed — genetics as code
  → S / fold coupling where needed  // seeds_fixed K, ψ_con, η_eff
  → W = fsotPairWeight              // genetic_fixed
  → FI / rates / ISI                // READOUT of expression
  → Allen / scalpel / CSV           // measure bridge, refine ORFs/expression laws
```

---

## 3. Codon genetics (archive certificate)

`codon_manifest.yaml`:

| Check | Value |
|-------|------:|
| codon_count | 64 |
| primary_pattern_space | 8 |
| secondary_pattern_space | 27 |
| stop_codons | TAA, TAG, TGA |
| start | ATG |
| coordinate_tolerance | 0 |

Map lives in archive Genetics + SR-ITE Zig driver; Zig mind must keep the same PRIMARY law (`codon.zig` + `data/64_codon_trinary_map.txt`).

---

## 4. What archive accuracy *means* vs Zig Allen FI gates

| Archive science panels | Zig mind FI gates |
|------------------------|-------------------|
| Pooled median **error % ≤ 0.5%** on tabulated observables under pin | Absolute residual **ms / Hz / \|ΔA\|** on Fixed 1 ms lattice |
| Domain fold \(D_{\mathrm{eff}}\), hits, δψ | Class ORFs + phenotype + network step |
| Live API measured authority | Allen Cell Types ephys (monorepo CSV / Cre means) |

They are **same law, different domain engines and metrics**. Archive GREEN does not auto-pass Zig every-cell ISI — but it **forbids** treating membrane FI as a free LSQ surface disconnected from seeds/codons.

NeuroLab pathway contract: **max mean abs gap 0.15** on brain fit dashboards (`neurolab_bio_manifest.yaml`) — use as pathway-level concordance style, not as excuse for free tables.

---

## 5. What Zig should pull *next* from the archive (implementation order)

1. **Keep law constants only from pin seeds** (`seeds_fixed` twin of `fsot_compute.py` L1/L2).  
2. **All FI populations from genetics** (`fillFromGenetics` / class ORFs) — already re-anchored.  
3. **Import control-profile spirit** from biological brain derivations into `pathways_fixed` gains (relay/ff/fb from expression coupling), not hardcoded mystery constants where avoidable.  
4. **Concordance κ-style scoring** for pathway outputs (activation / error / replay) alongside FI rates.  
5. **When Allen residual opens:** change **ORF / expression / phenotype law** (archive pattern: adaptive refinement on *program* under FSOT scalars), not anonymous `fi_stim` tables.  
6. Optionally ingest archive `brain_component_priors` / fit dashboard formats if files exist on monorepo Desktop paths for parity.

---

## 6. Files agents must open before inventing “new” bio math

| Path |
|------|
| `I:\FSOT-Physical-Archive\READ_ME_FIRST.md` |
| `I:\FSOT-Physical-Archive\VERIFICATION_REPORT.json` |
| `I:\FSOT-Physical-Archive\FSOT_USAGE_DOCTRINE.md` |
| `I:\FSOT-Physical-Archive\ARCHIVE_MATH_AND_VERIFICATION_DEEP_DIG.md` |
| `I:\FSOT-Physical-Archive\ZIG_MIND_AND_GITHUB_MAP.md` |
| `I:\FSOT-Physical-Archive\01_SR-ITE-USB-Original\6_unified_oracle\fsot_3_0\FSOT_BIOLOGICAL_BRAIN_MATHEMATICAL_DERIVATIONS.md` |
| `I:\FSOT-Physical-Archive\04_Genetics-Longevity\64_codon_trinary_map.txt` |
| `I:\FSOT-Physical-Archive\02_FSOT-2.1-Lean-Full\data\codon_manifest.yaml` |
| `I:\FSOT-Physical-Archive\02_FSOT-2.1-Lean-Full\data\neurolab_bio_manifest.yaml` |

---

## 7. Bottom line for this repo

You already **solved** the law spine, multi-prover identity, codon table, and an explicit **biological brain derivation stack** that couples **trinary/genomic signature → FSOT expression scalar → pathway gains → concordance**.

Zig’s job is the **Fixed genetic domain engine**:

- **code = 64-codon genetics + class ORFs**  
- **physics of cell = FSOT seeds / pair weights**  
- **Allen / CSV / scalpel = verification readout**

Free-parameter FI polish that ignores that stack fights the archive. Refinement that changes **genetic expression laws** under the pin is how the archive itself refined the biological brain program.
